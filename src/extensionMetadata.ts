/**
 * Minimum `mvs-manager` (MVSengine) version this extension is developed and CI-tested against.
 * Older CLIs may lack `lint --format json` or differ in JSON shape; upgrade from MVSengine releases.
 */
export const MIN_MVS_MANAGER_VERSION = "1.10.0";
